<?xml version="1.0" encoding="utf-8"?>
<!--SkyWorthVR plugin additions-->
<root xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- init section is always evaluated once per architecture -->
	<init>
    <trace enable="true"/>  <!--  for debugging script -->
		<log text="SkyWorthVRHMD init"/>
		<!-- currently support arm 32 and 64 bit -->
		<setBool result="bSupported" value="false"/>
    <isArch arch="armeabi-v7a">
      <setBool result="bSupported" value="true"/>
    </isArch>
    <isArch arch="arm64-v8a">
      <setBool result="bSupported" value="true"/>
    </isArch>
  </init>

	<!-- optional additions to the GameActivity class in GameActivity.java -->
	<gameActivityClassAdditions>
		<insert>
			/** Whether this application was packaged for SkyWorthVR or not */
			public boolean PackagedForSkyWorthVR = true;

			// check the manifest to determine if we are a SkyWorthVR application
			public boolean AndroidThunkJava_IsSVRApplication()
			{
			return PackagedForSkyWorthVR;
			}
		</insert>
	</gameActivityClassAdditions>
	
</root>
